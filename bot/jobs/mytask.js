let contador = 0;

function miTarea(data) {
  // Incrementar el contador en 1
  contador++;

  // Imprimir el valor del contador
  console.log('Contador:', contador);
  
  return '1wjijdwiij'
}

miTarea()